[project]
name = "claude-token-rest"
version = "1.0.0"
description = "Claude token usage in macOS menu bar"
requires-python = ">=3.11"
dependencies = [
    "rumps==0.4.0",
    "watchdog==4.0.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"
